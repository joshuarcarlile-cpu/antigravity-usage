# Antigravity Telemetry Standards

When the user asks for `/usage`, `/cost`, token statistics, context limits, or session expenses:
- Activate and run the `usage` skill.
- Provide the real-time telemetry card within a fenced markdown block to preserve mechanical box-drawing alignment.
